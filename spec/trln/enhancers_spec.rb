require 'spec_helper'

describe Trln::Enhancers do
  it 'has a version number' do
    expect(Trln::Enhancers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
